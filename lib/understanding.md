The step to follow the mvvm.

    Always build the model class first
    Then build the repository class to call the api and handle the data.
    Then build the View Model class
    Finally build the UI class.
